# Scenario 18 — hard customer Audit Record

Local only: `DS2_ENV_FILE=.env.scenarios node_modules/.bin/mocha --require test/setup.js test/integration/scenario-lifecycle-18-audit-record.integration.spec.js --exit --timeout 180000`. Also included automatically by `npm run -s test:scenarios`.

Hand oracle: work100 edited to120; retainer100 leaves debt120/available100. Funded work50 and draw50 leave debt120/available50. Write-off20 gives debt100. Refund10 then credit adjustment5 leave available45 and debt100. Draft changes nothing; finalize=sent locks100. Check30 gives70; finalize locks it; bounced-check reversal restores100. Duplicate work10+10 gives120, then remove10 gives110 (billed100/unbilled10), available45. Imports into review queues do not post money. Each stage checks hard history; selected stages also check Create Invoice, independent Audit and AR.

The scenario checks session attribution despite URL actor999, recorded name snapshots/system import source, prior-value preservation, transaction grouping, savepoint rollback/lock reacquisition, Phoenix range/opening balance and pagination, full PDF content/hash, identical reopening after later edits, private document identity, admin/staff/tenant/missing/validation/fault refusals with unchanged data and evidence, metadata guards, forged storage identity/mismatched anchor detection, PDF/chain tampering and reconstructed history including deletion. Tampering is performed only in the disposable scenario database and restored. The migration spec separately performs privileged tampering in disposable ds2_clean. No production/reference database writes or external services.

## Run 5: readable records and retained evidence

The lifecycle produces 22 transaction postings and 89 captured row changes before printing. The client PDF must fit at most six pages, include every posting and account for every change in human form, include every individually listed business field and reason, and contain no raw JSON or internal table names. Tests compare selected database event IDs and field keys with the coverage manifest, then confirm each individual description/field and summary in extracted PDF text. Supporting technical fields have explicit omission entries and remain in the immutable JSON source. USD uses `$1,234.56`, credit balances `-$1,234.56`; Debit/Credit magnitudes and available retainer funds are positive.

Both `client` (default) and `full_evidence` are tested through real routes and local write-once storage. Full evidence uses type/byte-length/SHA-256/JSON-Pointer descriptors for structured values, checked against the archive returned by the verified API. Tests cover type metadata/listing/header, both digests, exact reopening after later edits, failed PDF upload after successful source upload, invalid record types, archive corruption/wrong key/missing legacy archive, failed chain/anchor, and role/tenant/missing/DB/storage failures with no-write comparisons. Existing lock/rollback/capture/rolling-balance tests remain. There are 32 tests in this file.

## Run 6: archive summaries and plain verification

The same money oracle and 32 cases remain. The two finalizations archive 11 and 15 statement copies; the reversal adds 2. Tests require 3 client summary lines covering exactly those 28 changes, plus 61 individual changes, still totaling 89. Category counts are hand specified (first issue: 1 invoice balance, 2 work, 1 payment, 1 write-off, 4 retainers, 2 adjustments; second: 4, 2, 2, 1, 4, 2; reversal: 1 payment and 1 invoice balance). Each captured database event ID and change number must occur exactly once in the client coverage; every source field remains in the itemized manifest. The extracted PDF's individual headings plus summary counts must equal the database count. Full evidence must still print every individual heading and all 28 statement-copy entries with API retrieval paths. Client output must show plain verification wording and all three digests/anchor without API paths.

Current samples: [Client record](../decisions/evidence/run-6/client-record.pdf) and [Full evidence record](../decisions/evidence/run-6/full-evidence-record.pdf), with their metadata and exact evidence archives alongside. Samples are retained before disposable resets; their local API IDs may no longer exist after a later reset. Page rendering and full acceptance are in the [run-6 results](../decisions/2026-09-25-run-6-results.md). Prior [run-5 results](../decisions/2026-09-25-run-5-results.md) and samples are preserved.
