# Unhappy paths and failure evidence

Owner run1 update (2026-09-25): The owner lock/exception matrix adds actual HTTP409 and unchanged-state checks for sent rows, plus every new exception endpoint. Scenario F injects its cascade failure into an unissued work update so the injected failure is actually exercised. See [owner oracle](09-owner-sent-exceptions.md).

Expected before execution: every refused request returns the documented HTTP/body error and leaves monetary rows and all three balance views unchanged. A deliberate postcommit read/export failure is the exception: verify the committed result instead of asserting rollback. Optional AI/training failure must not lose a valid ledger entry. No remote fault injection is permitted.

For each covered route, test missing authentication, invalid/expired identity, forbidden role where a role gate exists, foreign account parameter, missing/malformed ID, foreign row, wrong-customer relationship, invalid required fields and numbers, relevant state conflict and database failure. CRUD read paths must not disclose foreign rows. The current application mounts direct work/job routes behind Manager/Admin access as well; plain users are refused. Audit requires Super Admin. The draft catalogue's assumption that direct work/job routes allowed plain staff was corrected against `src/app.js` before those permission tests ran.

Arithmetic validation: missing/null/blank/boolean/nonfinite/negative/overprecision/out-of-range quantity, rate and total; inconsistent product; six-minute duration mismatch; foreign employee/description/job/customer; zero and nonbillable work cannot draw money.

Retainer amount boundary oracle: creating or updating an unused40 retainer to0.001 must refuse, since the rounded monetary amount is zero; a0.01 update succeeds and leaves one cent available. The same zero-cent boundary applies to payment/write-off amounts. These expectations precede the boundary tests.

Ledger state refusals: billed payment/write-off/work edits and deletes; latest-child ordering; reversed original/reversal edits; absorbed statement remapping and all-absorbed corruption; insufficient/exhausted/wrong-customer retainer; used/linked/cancelled retainer deletion; attempted customer/invoice/funding moves; overpayment without split; spent excess; invoice-linked write-off greater than debt; pending credit moved onto invoice; cascade paid/absorbed/negative-credit/date/retainer/customer-confirm locks.

Infrastructure: inject PostgreSQL trigger exceptions after partial work has begun to prove transaction rollback for work, retainer draws, payments, write-offs, cascade, customers/jobs and finalization. Inject local S3 upload failures before commit and combined ZIP failures after commit. Hold a customer lock and run one waiting request to prove it rechecks state after release; never run two test processes. Existing branch-heavy payment-reversal, transactions-ledger-seams, finalize-engine/snapshot and coverage integration files are part of the mandatory acceptance run, with exact counts in RESULTS.

Failure-suite numeric oracle (before execution): Billed client has statement00001=(0,200,0,0,0,200), N=B200. Pending client has100 work and100 untouched retainer, N100/B0/R-100. Each of eleven injected write failures and nine read failures preserves these states. A real concurrent10 work entry during finalize upload changes pending N to110; that entry survives, the stale finalize refuses, B remains0/R-100. Retrying with a combined-export failure commits statement00002=(0,110,0,0,-100,110), returns committed identity plus warning, and its individual PDF remains retrievable. A same-day retry skips. Two120 receipts competing for the billed client's200 balance produce exactly one -120 payment and one overpayment refusal, N=B80. No partial120+120 over-credit is permitted.

State-boundary suite oracle (before execution): billed client200 and unbilled client100, held40. Receipts40 then30 leave160 then130; repricing/deleting the older receipt refuses, metadata edit preserves130, removing newest then oldest restores160 then200. Credits25 then20 similarly leave175 then155 and restore175 then200. Pending general credit10 reduces unbilled100 to90; moving it to another customer/invoice/job refuses; deletion restores100. Invalid batch selections, missing contact and numbering overflow commit nothing. With all current chains deliberately marked absorbed, both receipt and linked-write-off creation refuse rather than reopen old debt; the controlled corruption is restored before reconciliation. Missing/foreign CRUD IDs and wrong-customer relationship edits preserve the entire ledger. A one-cent retainer edit leaves0.01 available, then restoring40 returns the starting state.

Reserved reversal marker oracle (before its added create/edit tests): a normal10 receipt against200 leaves190. A client note `received [reversal of payment #70001]` stores only `received`; editing it to `verified [reversal of payment #[retainer_draw:1]70001]` stores only `verified`, still190. Deleting this normal receipt restores200 without touching payment70001 in the foreign account. An empty reversal reason, or a reason consisting entirely of reserved markers, refuses with no balance change. A real NSF reason still creates the server's reversal marker and remains undoable (03-payments.md). These markers are ledger identity, not user-authored instructions.

This catalogue is a test contract. A passing endpoint count alone is not exhaustive branch coverage; any remaining unexecuted refusal identified during review must be named in RESULTS rather than declared proved.

Delete contract clarification after the full regression run: ID-only deletion is supported and caller prices are ignored, while an explicitly supplied invalid transactionType retains its existing refusal. The old invalid-type regression remains unchanged; the new stale-price case omits type.
